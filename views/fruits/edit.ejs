<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Fruit</title>
</head>



<body
        <a href="/fruits">Back to Fruits</a>
        <h1>Edit <%= fruit.name %></h1>
       <form action ="/fruits/<%=fruit._id%>?_method=PUT" method="POST">     <!-- using methodOverride -->
            <label for="name">Name:</label>
            <input type="text" name="name" value="<%= fruit.name %>" />

            <label for="isReadyToEat">Ready to Eat? </label>
            <input type="checkbox" 
                   name="isReadyToEat" 
                   id="isReadyToEat"
                   <% if (fruit.isReadyToEat) { %> checked <% } %> >    <!-- Checked by default -->
                   

            <button type="submit">Update Fruit</button>
        </form>

        
</body>
</html>